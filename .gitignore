# Secrets — real config with API keys, meter serials, passwords.
# Keep only the template in git.
ha-addon/config.py
config.py
ha-addon/options.json

# Local backup of the pre-restructure files and secrets — never publish
_legacy/

# Local Developer Setup & Implementation Docs — keep local, never publish
LOCAL_SETUP.md
DEVELOPMENT.md

# Python & Environment
__pycache__/
*.pyc
*.pyo
*.pyd
.python-version
.venv/
venv/
ENV/
env/

# Editors & Operating System
.vscode/
.idea/
.DS_Store
*.swp
*~

# Test, Coverage & Log Artefacts
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
*.log
test-results/
junit-*.xml

# Temporary & State Files
*.tmp
*.bak
state.json
